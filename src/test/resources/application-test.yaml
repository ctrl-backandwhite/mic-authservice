test:
  postgres:
    username: test_user
    password: test_password
    database: test_db

app:
  jwt:
    secret: local-secret-key-change-me-in-production-must-be-256-bits-long

logging:
  level:
    com.backandwhite.common.exception.Message: ERROR
    com.backandwhite.api.exception.GlobalExceptionHandler: ERROR

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
