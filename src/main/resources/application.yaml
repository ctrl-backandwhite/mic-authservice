spring:
  application:
    name: mic-authservice
  banner:
    charset: UTF-8
  profiles:
    active: ${PROFILE:local},kibana
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=500,expireAfterAccess=600s
